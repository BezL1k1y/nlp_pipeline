# nlp_pipeline
Three components from scratch: a custom tokenizer with a dictionary and BOS/EOS, a TF-IDF vectorizer with L2-normalization, and a trigram language model with greedy and sampled generation. The demo runs everything on Stanford IMDB (accuracy ≥ 0.80).
